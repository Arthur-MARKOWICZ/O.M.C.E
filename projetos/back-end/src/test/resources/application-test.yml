spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3307/omce_test
    username: usuario
    password: senha
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  test:
    database:
      replace: none
  flyway:
    enabled: true
    out-of-order: true