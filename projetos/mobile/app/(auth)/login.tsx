import { useState } from 'react';
import { Text } from 'react-native';
import { Link, useRouter } from 'expo-router';
import { AuthShell } from '@/src/components/AuthShell';
import { Button, Field, Message } from '@/src/components/ui';
import { api } from '@/src/lib/api';
import { useApp } from '@/src/context/AppContext';
export default function Login() { const router = useRouter(); const { signIn } = useApp(); const [email, setEmail] = useState(''); const [senha, setSenha] = useState(''); const [error, setError] = useState(''); const [saving, setSaving] = useState(false); const submit = async () => { if (!email || !senha) return setError('Informe o e-mail e a senha.'); setSaving(true); setError(''); try { await signIn(await api.login(email, senha)); router.replace('/(tabs)'); } catch (e) { setError(e instanceof Error ? e.message : 'Não foi possível entrar.'); } finally { setSaving(false); } }; return <AuthShell title="Acesse sua conta" subtitle="Entre para comprar, vender e criar.">{error && <Message>{error}</Message>}<Field label="E-mail" value={email} onChangeText={setEmail} autoCapitalize="none" keyboardType="email-address" autoComplete="email" /><Field label="Senha" value={senha} onChangeText={setSenha} secureTextEntry autoComplete="password" /><Button onPress={submit} disabled={saving}>{saving ? 'Entrando…' : 'Entrar'}</Button><Text style={{ textAlign: 'center', fontFamily: 'Manrope_400Regular' }}><Link href="/(auth)/redefinir-senha" style={{ color: '#176b42', fontFamily: 'Manrope_800ExtraBold' }}>Esqueci minha senha</Link></Text><Text style={{ textAlign: 'center', fontFamily: 'Manrope_400Regular' }}>Ainda não tem conta? <Link href="/(auth)/cadastro" style={{ color: '#176b42', fontFamily: 'Manrope_800ExtraBold' }}>Cadastre-se</Link></Text></AuthShell>; }
