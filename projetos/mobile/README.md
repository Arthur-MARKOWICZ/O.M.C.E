# O.M.C.E Mobile

Aplicativo Expo/React Native do mercado de componentes eletrônicos O.M.C.E.

1. Copie `.env.example` para `.env` e informe a URL alcançável do backend.
2. Execute `pnpm install` e depois `pnpm start`.
3. Use `pnpm android` ou `pnpm ios` para abrir o app.

