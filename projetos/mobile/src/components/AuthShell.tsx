import { PropsWithChildren } from 'react';
import { Pressable, StyleSheet, Text, View } from 'react-native';
import { Link } from 'expo-router';
import { Ionicons } from '@expo/vector-icons';
import { Screen } from '@/src/components/ui';
import { useApp } from '@/src/context/AppContext';
export function AuthShell({ title, subtitle, children }: PropsWithChildren<{ title: string; subtitle: string }>) { const { colors, theme, toggleTheme } = useApp(); return <Screen scroll={true}><View style={styles.top}><Link href="/(auth)/login" asChild><Pressable><Text style={[styles.brand, { color: colors.ink }]}><Text style={{ color: colors.green }}>◈ </Text>O.M.C.E</Text></Pressable></Link><Pressable onPress={toggleTheme}><Ionicons name={theme === 'dark' ? 'sunny-outline' : 'moon-outline'} size={23} color={colors.ink} /></Pressable></View><View style={[styles.card, { backgroundColor: colors.surface, borderColor: colors.line }]}><Text style={[styles.eyebrow, { color: colors.green }]}>MERCADO DE ELETRÔNICOS</Text><Text style={[styles.title, { color: colors.ink }]}>{title}</Text><Text style={[styles.subtitle, { color: colors.muted }]}>{subtitle}</Text>{children}</View><Text style={[styles.aside, { color: colors.green }]}>Componentes, projetos e possibilidades.{"\n"}<Text style={{ color: colors.ink }}>Todo circuito começa com uma peça.</Text></Text></Screen>; }
const styles = StyleSheet.create({ top: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', marginTop: 7 }, brand: { fontFamily: 'Manrope_800ExtraBold', fontSize: 19, letterSpacing: 1 }, card: { borderWidth: 1, borderRadius: 10, padding: 21, gap: 15, marginTop: 40 }, eyebrow: { fontFamily: 'DMMono_500Medium', fontSize: 10, letterSpacing: 1 }, title: { fontFamily: 'PlayfairDisplay_700Bold', fontSize: 35, lineHeight: 41 }, subtitle: { fontFamily: 'Manrope_400Regular', fontSize: 14, lineHeight: 21, marginBottom: 4 }, aside: { fontFamily: 'PlayfairDisplay_600SemiBold', fontSize: 22, lineHeight: 29, marginTop: 36 } });
